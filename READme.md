# IT Tupolev Challenge 2026
> Хакатон # IT Tupolev Challenge 2026 21 - 24 февраля

## 🛠 Стек технологий

| Компонент | Технология |
|---|---|
| **Backend** | FastAPI, Python 3.9+ |
| **БД** | PostgreSQL 15 |
| **ORM** | SQLAlchemy |
| **Миграции** | Alembic |
| **Аутентификация** | JWT (python-jose) |
| **Валидация** | Pydantic |
| **Сервер** | Uvicorn |
| **Docker** | Docker, Docker Compose |
| **Линтинг** | Ruff |

## 📋 Требования для запуска

- Docker
- Docker Compose


## 🚀 Быстрый старт

#### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd Tupolev_IT_Challenge_2026
```

#### 2. Создание файла `.env`
Копируем файл `.env.example`
```bash
cp .env.example .env
```

#### 3. Запуск контейнеров
```bash
docker compose up -d
```

Сервер будет доступен по адресу: `http://localhost:8000`
Также можно посмотреть документацию `http://localhost:8000\docs`

#### 4. Просмотр логов
```bash
docker compose logs -f *api
docker compose logs -f *postgres
```

#### 5. Остановка контейнеров
```bash
docker-compose down
```

---

## 📊 Управление базой данных

### Применение миграций
```bash
docker-compose exec api alembic upgrade head
```

### Просмотр текущего состояния
```bash
docker-compose exec api alembic current
```

### Откат последней миграции
```bash
docker-compose exec api alembic downgrade -1
```

### Создание новой миграции
```bash
docker-compose exec api alembic revision --autogenerate -m "Описание изменений"
```

---

## 📁 Структура проекта

```
.
├── alembic/                    # Миграции БД
│   ├── versions/              # Файлы версий миграций
│   ├── env.py                 # Конфигурация Alembic
│   └── alembic.ini            # Файл конфигурации
├── app/                       # Основное приложение
│   ├── api/                   # API endpoints
│   │   ├── dependencies.py    # Зависимости для endpoints
│   │   └── endpoints/         # Маршруты
│   │       └── auth.py        # Аутентификация
│   ├── core/                  # Основные модули
│   │   ├── config.py          # Конфигурация приложения
│   │   ├── database.py        # Подключение к БД
│   │   └── security.py        # Логика безопасности
│   ├── models/                # ORM модели
│   │   └── user.py            # Модель пользователя
│   ├── schemas/               # Pydantic схемы
│   │   ├── token.py           # Схема токена
│   │   └── user.py            # Схема пользователя
│   ├── services/              # Бизнес-логика
│   │   └── user.py            # Сервис пользователя
│   └── main.py                # Точка входа приложения
├── tests/                     # Тесты
├── docker-compose.yml         # Конфигурация Docker Compose
├── Dockerfile                 # Docker конфигурация
├── requirements.txt           # Зависимости проекта
└── .env.example              # Пример переменных окружения
```

---

## 🔐 API Endpoints

| Метод | Endpoint | Описание | Требует Auth |
|---|---|---|---|
| **GET** | `/` | Главная страница | Нет |
| **GET** | `/health` | Проверка здоровья сервера | Нет |
| **POST** | `/api/auth/register` | Регистрация нового пользователя | Нет |
| **POST** | `/api/auth/login` | Вход в систему | Нет |

---

## 🛠️ Разработка

### Линтирование кода
```bash
docker-compose exec api ruff check app/
```

### Форматирование кода
```bash
docker-compose exec api ruff format app/
```

---

## 📝 Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|---|---|---|
| `POSTGRES_USER` | Пользователь PostgreSQL | - |
| `POSTGRES_PASSWORD` | Пароль PostgreSQL | - |
| `POSTGRES_DB` | Имя базы данных | - |
| `POSTGRES_PORT` | Порт PostgreSQL | 5432 |
| `DATABASE_URL` | URL подключения к БД | - |
| `DEBUG` | Режим отладки | false |

---

## 🐛 Решение проблем

### Ошибка подключения к БД
1. Убедитесь, что контейнеры запущены: `docker-compose ps`
2. Проверьте логи: `docker-compose logs postgres`
3. Перезагрузите контейнеры: `docker-compose restart`

### Ошибки при миграции
```bash
# Проверить текущую версию
docker-compose exec api alembic current

# Откатить до базовой версии и применить заново
docker-compose exec api alembic downgrade base
docker-compose exec api alembic upgrade head
```

### Проблемы с Docker
```bash
# Пересобрать образы
docker-compose down -v
docker-compose up --build -d

# Просмотреть логи
docker-compose logs -f
```

### Очистка контейнеров полностью
```bash
docker-compose down -v
docker volume prune
docker image prune
docker-compose up -d
```